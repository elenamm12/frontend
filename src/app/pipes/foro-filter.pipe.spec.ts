import { ForoFilterPipe } from './foro-filter.pipe';

describe('ForoFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ForoFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
